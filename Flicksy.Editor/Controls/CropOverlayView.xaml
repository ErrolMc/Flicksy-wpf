<UserControl x:Class="Flicksy.Editor.Controls.CropOverlayView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:Flicksy.Editor.ViewModels"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=vm:CropOverlayViewModel, IsDesignTimeCreatable=False}"
             Background="{x:Null}"
             IsHitTestVisible="True">
    <Grid x:Name="OverlayRoot" Background="Transparent">
        <Canvas x:Name="OverlayCanvas" Background="{x:Null}">
            <!-- Dim regions over the image area outside the crop rect -->
            <Rectangle x:Name="TopShade" Fill="#99000000" IsHitTestVisible="False" />
            <Rectangle x:Name="LeftShade" Fill="#99000000" IsHitTestVisible="False" />
            <Rectangle x:Name="RightShade" Fill="#99000000" IsHitTestVisible="False" />
            <Rectangle x:Name="BottomShade" Fill="#99000000" IsHitTestVisible="False" />

            <!-- Thin white outline around the crop rectangle -->
            <Rectangle x:Name="CropOutline"
                       Stroke="White"
                       StrokeThickness="1"
                       Fill="Transparent"
                       IsHitTestVisible="False"
                       SnapsToDevicePixels="True" />

            <!-- Corner brackets (thick white L-shapes inside each corner) -->
            <Polyline x:Name="TopLeftBracket"
                      Stroke="White"
                      StrokeThickness="3"
                      StrokeLineJoin="Miter"
                      IsHitTestVisible="False" />
            <Polyline x:Name="TopRightBracket"
                      Stroke="White"
                      StrokeThickness="3"
                      StrokeLineJoin="Miter"
                      IsHitTestVisible="False" />
            <Polyline x:Name="BottomLeftBracket"
                      Stroke="White"
                      StrokeThickness="3"
                      StrokeLineJoin="Miter"
                      IsHitTestVisible="False" />
            <Polyline x:Name="BottomRightBracket"
                      Stroke="White"
                      StrokeThickness="3"
                      StrokeLineJoin="Miter"
                      IsHitTestVisible="False" />

            <!-- Edge midpoint markers (short white bars centered on each edge) -->
            <Rectangle x:Name="TopEdgeMarker" Fill="White" IsHitTestVisible="False" />
            <Rectangle x:Name="BottomEdgeMarker" Fill="White" IsHitTestVisible="False" />
            <Rectangle x:Name="LeftEdgeMarker" Fill="White" IsHitTestVisible="False" />
            <Rectangle x:Name="RightEdgeMarker" Fill="White" IsHitTestVisible="False" />
        </Canvas>
    </Grid>
</UserControl>
