<UserControl x:Class="Flicksy.PostSnip.Controls.VideoPlaybackOverlay"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             MinHeight="56">
    <UserControl.Resources>
        <SolidColorBrush x:Key="OverlayBackgroundBrush" Color="#CC1F1F1F" />
        <SolidColorBrush x:Key="OverlayBorderBrush" Color="#4DFFFFFF" />
        <SolidColorBrush x:Key="ControlBackgroundBrush" Color="#333333" />
        <SolidColorBrush x:Key="ControlHoverBrush" Color="#3F3F3F" />
        <SolidColorBrush x:Key="ControlPressedBrush" Color="#2B2B2B" />
        <SolidColorBrush x:Key="ControlBorderBrush" Color="#55FFFFFF" />
        <SolidColorBrush x:Key="TextBrush" Color="#FFFFFF" />
        <SolidColorBrush x:Key="SliderTrackBrush" Color="#4DFFFFFF" />
        <SolidColorBrush x:Key="SliderProgressBrush" Color="#60CDFF" />
        <SolidColorBrush x:Key="SliderThumbFillBrush" Color="#FFFFFF" />
        <SolidColorBrush x:Key="SliderThumbBorderBrush" Color="#80000000" />

        <Style x:Key="OverlayButtonStyle" TargetType="Button">
            <Setter Property="Foreground" Value="{StaticResource TextBrush}" />
            <Setter Property="Background" Value="{StaticResource ControlBackgroundBrush}" />
            <Setter Property="BorderBrush" Value="{StaticResource ControlBorderBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="14,0" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="ButtonBorder"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6"
                                SnapsToDevicePixels="True">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"
                                              RecognizesAccessKey="True" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Background" Value="{StaticResource ControlHoverBrush}" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Background" Value="{StaticResource ControlPressedBrush}" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="ButtonBorder" Property="Opacity" Value="0.5" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="OverlaySliderThumbStyle" TargetType="Thumb">
            <Setter Property="Width" Value="12" />
            <Setter Property="Height" Value="12" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Thumb">
                        <Ellipse Fill="{StaticResource SliderThumbFillBrush}"
                                 Stroke="{StaticResource SliderThumbBorderBrush}"
                                 StrokeThickness="1" />
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="OverlaySliderStyle" TargetType="Slider">
            <Setter Property="Height" Value="24" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Slider">
                        <Grid Margin="0,6,0,6">
                            <Track x:Name="PART_Track"
                                   IsDirectionReversed="{TemplateBinding IsDirectionReversed}"
                                   Maximum="{TemplateBinding Maximum}"
                                   Minimum="{TemplateBinding Minimum}"
                                   Value="{Binding RelativeSource={RelativeSource TemplatedParent}, Path=Value, Mode=TwoWay}">
                                <Track.DecreaseRepeatButton>
                                    <RepeatButton Focusable="False"
                                                  IsTabStop="False"
                                                  Command="Slider.DecreaseLarge">
                                        <RepeatButton.Template>
                                            <ControlTemplate TargetType="RepeatButton">
                                                <Border Height="4"
                                                        VerticalAlignment="Center"
                                                        Background="{StaticResource SliderProgressBrush}"
                                                        CornerRadius="2" />
                                            </ControlTemplate>
                                        </RepeatButton.Template>
                                    </RepeatButton>
                                </Track.DecreaseRepeatButton>
                                <Track.Thumb>
                                    <Thumb Style="{StaticResource OverlaySliderThumbStyle}" />
                                </Track.Thumb>
                                <Track.IncreaseRepeatButton>
                                    <RepeatButton Focusable="False"
                                                  IsTabStop="False"
                                                  Command="Slider.IncreaseLarge">
                                        <RepeatButton.Template>
                                            <ControlTemplate TargetType="RepeatButton">
                                                <Border Height="4"
                                                        VerticalAlignment="Center"
                                                        Background="{StaticResource SliderTrackBrush}"
                                                        CornerRadius="2" />
                                            </ControlTemplate>
                                        </RepeatButton.Template>
                                    </RepeatButton>
                                </Track.IncreaseRepeatButton>
                            </Track>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Border Background="{StaticResource OverlayBackgroundBrush}"
            BorderBrush="{StaticResource OverlayBorderBrush}"
            BorderThickness="1"
            CornerRadius="10"
            Margin="12"
            Padding="10">
        <Grid VerticalAlignment="Bottom">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="12" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="12" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <Button x:Name="PlayPauseButton"
                    Width="84"
                    Height="32"
                    Style="{StaticResource OverlayButtonStyle}"
                    Click="OnPlayPauseClick"
                    Content="Play" />

            <Slider x:Name="TimelineSlider"
                    Grid.Column="2"
                    Style="{StaticResource OverlaySliderStyle}"
                    Minimum="0"
                    Maximum="0"
                    Value="0"
                    IsMoveToPointEnabled="True"
                    VerticalAlignment="Center"
                    PreviewMouseLeftButtonDown="OnSliderMouseDown"
                    PreviewMouseLeftButtonUp="OnSliderMouseUp"
                    ValueChanged="OnSliderValueChanged" />

            <TextBlock x:Name="TimeText"
                       Grid.Column="4"
                       VerticalAlignment="Center"
                       Foreground="{StaticResource TextBrush}"
                       FontSize="12"
                       FontFamily="Consolas"
                       Text="00:00 / 00:00" />
        </Grid>
    </Border>
</UserControl>
